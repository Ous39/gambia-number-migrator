import { FormEvent,useEffect,useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { ArrowRight,Eye,EyeOff,LockKeyhole,ShieldCheck } from 'lucide-react';
import { api,setAdmin,setToken } from '../api/client';
export default function Login(){const navigate=useNavigate();const[username,setUsername]=useState('admin');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[error,setError]=useState('');const[busy,setBusy]=useState(false);useEffect(()=>{document.body.classList.remove('light');},[]);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{const res=await api<{token:string;admin:any}>('/auth/login',{method:'POST',body:JSON.stringify({username,password})});setToken(res.token);setAdmin(res.admin);navigate('/');}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <div className="loginPage"><div className="loginBrand"><div className="loginBrandMark">GN</div><span className="heroKicker">OCEANBROWN PLATFORM</span><h1>Gambia Number<br/>Migration Console</h1><p>Secure operational control for verified migration rules, transition settings, payments and user support.</p><div className="loginTrust"><ShieldCheck size={20}/><span><b>Privacy-first administration</b><small>Contact lists never enter this console</small></span></div><small className="loginPowered">Powered by OceanBrown</small></div><form className="loginBox" onSubmit={submit}><div className="loginIcon"><LockKeyhole size={25}/></div><span className="eyebrow">SECURE ADMIN ACCESS</span><h2>Welcome back</h2><p>Sign in using your individual team account.</p>{error&&<p className="loginError">{error}</p>}<label>Username<input className="input" autoComplete="username" required value={username} onChange={e=>setUsername(e.target.value)}/></label><label>Password<div className="passwordField"><input className="input" type={show?'text':'password'} autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShow(!show)} aria-label={show?'Hide password':'Show password'}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><button className="btn loginButton" disabled={busy}>{busy?'Signing in…':<>Sign in securely <ArrowRight size={18}/></>}</button><small className="loginHelp">Access is logged for security and accountability.</small></form></div>}
