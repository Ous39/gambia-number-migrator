import { useEffect,useMemo,useState } from 'react';
import { CreditCard,Search,ShieldAlert,WalletCards } from 'lucide-react';
import { api } from '../api/client';
export default function Payments(){const[items,setItems]=useState<any[]>([]);const[msg,setMsg]=useState('');const[query,setQuery]=useState('');const load=()=>api<any>('/admin/payments').then(r=>setItems(r.data));useEffect(()=>{load().catch(e=>setMsg(e.message));},[]);const filtered=useMemo(()=>{const q=query.toLowerCase();return q?items.filter(x=>`${x.reference} ${x.provider} ${x.status} ${x.device_id}`.toLowerCase().includes(q)):items},[items,query]);async function confirm(id:string){setMsg('');try{await api('/admin/payments/'+id+'/confirm-manual',{method:'POST'});setMsg('Test payment confirmed and device access updated.');await load();}catch(e:any){setMsg(e.message)}}const successful=items.filter(x=>x.status==='success').length;return <div className="adminPage"><div className="topbar"><div className="pageTitle"><span className="eyebrow">PAYMENT OPERATIONS</span><h1>Payments</h1><p>Review test transactions and prepare for server-verified Wave, APS and store billing.</p></div><span className="contextBadge warning"><ShieldAlert size={17}/>Test mode</span></div>{msg&&<p className="notice">{msg}</p>}<div className="summaryGrid"><div className="summaryCard"><WalletCards size={22}/><span><small>Total intents</small><b>{items.length}</b></span></div><div className="summaryCard"><CreditCard size={22}/><span><small>Successful</small><b>{successful}</b></span></div><div className="summaryCard"><ShieldAlert size={22}/><span><small>Pending review</small><b>{items.filter(x=>x.status==='pending').length}</b></span></div></div><div className="notice warningNotice"><ShieldAlert size={20}/><span><b>Local testing only</b> Manual confirmation must be disabled when live Wave, APS, Apple or Google verification is enabled.</span></div><section className="card dataCard"><div className="tableToolbar"><div><h2>Transaction history</h2><p>Payment references and entitlement status.</p></div><label className="searchBox"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search reference, provider or status"/></label></div><div className="tableWrap"><table><thead><tr><th>Reference</th><th>Provider</th><th>Feature</th><th>Amount</th><th>Status</th><th>Created</th><th>Action</th></tr></thead><tbody>{filtered.map(p=><tr key={p.id}><td><code>{p.reference}</code></td><td><strong>{String(p.provider).toUpperCase()}</strong></td><td>{humanize(p.feature_key)}</td><td><strong>{p.amount} {p.currency}</strong></td><td><span className={`statusBadge ${p.status}`}>{p.status}</span></td><td>{new Date(p.created_at).toLocaleString()}</td><td>{p.status!=='success'?<button className="btn compact" onClick={()=>confirm(p.id)}>Confirm test</button>:<span className="completedLabel">Confirmed</span>}</td></tr>)}</tbody></table></div>{!filtered.length&&<div className="emptyState"><CreditCard size={28}/><b>No matching payments</b><span>New payment intents will appear here.</span></div>}</section></div>}
function humanize(v:string){return String(v||'').replace(/_/g,' ')}
