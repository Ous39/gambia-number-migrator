# v2.8.0 Screenshot Status

Screenshots were not fabricated. The build/export completed, but this environment did not provide a seeded authenticated staging API plus Android/iOS emulator or physical device. Those are required to capture every real state without placeholder production data.

Run `node scripts/capture-screenshots.mjs` after installing Playwright and starting the exported mobile web app and Admin. Native-only contact/permission/notification screens must additionally be captured from physical Android and iOS builds.
